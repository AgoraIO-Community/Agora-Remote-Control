## Example
# wheterh import `proj.android` project
buildWithAgoraProjAndroid=true
# exclude abi, for speed, default false
# this config shared by `proj.android` and `ut`
abiExcludeArm=true
abiExcludeArm64=false
abiExcludeX86=true
# when import `proj.android`, `proj.android` will read this prop
buildWithNative=true
# when import `proj.android`, `proj.android` will read this prop
gnoutDir=/Users/why/Agora/build
